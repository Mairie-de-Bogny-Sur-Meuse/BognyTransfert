RewriteEngine On

# Répertoire racine du projet (si besoin)
#RewriteBase /var/www/dl.bognysurmeuse.fr/www/

# Si le fichier ou le dossier n'existe pas, redirige tout vers index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
